<%= form_for([current_user, Item.new]) do |f| %>
  <div class='form-group'>
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: 'Add item.' %>
  </div>
  <div class='form-group'>
    <%= f.submit "Save", class: 'btn btn-success'%>
<% end %>
